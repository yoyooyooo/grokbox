version: 2
updates:
  # npm updates remain maintainer-driven because bun.lock is the authoritative lockfile.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
