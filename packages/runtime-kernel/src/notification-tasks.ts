/** Portable maintenance-task data and receipt contract, without an executor,
 * native IO, credentials, route selection or mutation authority. */
export * from "./internal/observation/notification-task.ts";
