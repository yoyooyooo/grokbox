// The ordinary Box-local lifecycle executor has been retired. Formal clone,
// replace, spawn and resume commands use management-api.ts and the Server's
// shared lifecycle program; this module intentionally exports no writer.
export {};
