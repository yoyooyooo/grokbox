// The direct current-state CLI writer is retired. Public commands enter the
// shared management API; CONT still owns native application and recovery.
// No legacy alias, fallback or exported mutation remains here.
export {};
