[workspace]
members = ["crates/host-verifier"]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.85.0"
license = "MIT"

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
panic = "abort"
