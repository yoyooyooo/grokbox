# AH-115 E1 准备回执

- Issue: AH-115
- Route: `grokbox-parallel-20260922/E1-P`
- Base tip: `06300394`
- Scope: monitor-service、原 monitor/outbox/receiver、保护 service 装配；不接管 CONT 恢复材料 writer。

## 本次交付

管理服务 `/v1/service` 现在返回三个受管理的 worker 清单：`monitor`、`notification-outbox`、`protection`。每项明确 owner、是否已启动、当前状态、阻塞原因、资格判断和受控副作用类别。collector 在 stale、gap、源不可用、native health 不完整或自身退出后都不会继续报告 `running`。

同一响应返回 `effectivePolicy`：观察目标数和通知模式、通知是否需要授权且不直连 native、默认保护目标和策略 revision，以及诊断容量接纳实际覆盖的四类 writer（`monitor`、`monitor-initialize`、`journal`、`process`）。`installationBudgetEnforced=false` 保留，表示当前是协作 writer 的有限接纳门，不是全安装或 OS quota。

未资格的自动副作用仍由原 owner 的配置和权限边界控制：通知 worker 保持授权门，保护 worker 保持 ownership/source 资格门；服务启动不会因核心服务前移而关闭必要观察或保护。

## 验证

- `bun test packages/client/test/observation-contract.test.ts`: 5 pass
- `bun test ./packages/server/test/observations.node.ts`: 13 pass
- `bun test packages/box-runtime/test/monitor-service-lifetime.test.ts packages/box-runtime/test/diagnostic-admission.test.ts`: 14 pass
- `bun run typecheck`: pass
- native-vs-fixture: 本轮使用仓库真实 runtime、SQLite、进程生命周期和 HTTP fixture；未宣称真实 Host/modeld 现场资格。

## 交付接口与剩余阻塞

- delivered interfaces: `ManagementServiceView.workers`、`ManagementServiceView.effectivePolicy`、monitor detector-exit 降级语义、容量 writer roster。
- merged-v2-sha: 待 Q 合回 v2。
- remaining blockers: 全安装跨 owner 物理预留、OS 启动注册、真实 native 关联与健康整合仍分别由后续 E1-I/LIVE 证据拥有；本票不把这些现场项改写为代码已完成。
