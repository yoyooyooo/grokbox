# v2 并行成果回流核对

2026-09-22。目标是把已完成的 v2 工作收进 `feat/box-runtime-v2`。本轮集成代码固定在 `d0363038c58d1f2f5f3b2686c0d500825f6f32c1`；本报告是其后的文档补充。检查覆盖本地分支、detached 工作树及现存 stash，不能仅以旧 SHA 是否为祖先判断代码是否遗漏。

## 已收录的成果

| 来源 | v2 中的落点与核对方式 |
| --- | --- |
| AH-134 导航、恢复与 Web 覆盖 | `a2b55d80`、`937d3a0e` 已是祖先；本轮不重复实现。 |
| AH-101 桌面文档 | 原 `42fd482f` 已移植为 `6f8abcd7`；后续工作保留在当前系统命令边界。 |
| AH-111、AH-113、AH-116 | `d0cc40ae`、`c7505c4c`、`2bb13015` 均已收录。 |
| AH-114、AH-118 消息 | `e4bc4a01` 已经合流；当前单套消息 API 由 `d4a8c579`、`2e84180c` 及 `f622fb88`、`d31ea283`、`14c59706` 延续。没有恢复重复的 CLI 注册或第二套消息存储。 |
| detached 消息、材料、CLI 测试 | `c2b340ae` → `d4a8c579`；`f1ab90f9` → `2e84180c`；`14fc40bb` → `10fc4d7d`；`e1322696` → `f1e1f28f`。 |
| hot-ledger 分支 | `bf767fd5`、`0ffcec62`、`53d6d0d6` 对应 `98be3020`、`e8fe81ea`、`435bd807`。range-diff 显示运行代码相同，区别为作者及文档脱敏；不重新导入脱敏前历史。 |
| tool-contract 分支 | 核心实现已由 `91dc3293` 等吸收。本轮 `ea7bc17e` 补回遗漏的 tool-choice 原因、当前工具声明说明和精确 STEP 排查命令；保留 `4ffdd760` 的等价声明错误文案。 |
| AH-133 self-reset | 原 detached `e2a674a7` 已整合为 `6d3e8acd`，并附 `c4e2765b` 合同与验证记录。本轮解决同一 agent 归属检查冲突后得到相同 Git tree，采用原工作分支的正式提交。 |
| AH-157 已完成核心配对阶段 | 本轮结束前新增的 `a68eb878` 经 `d0363038` 合流；保留报告对原生消息资格未完成的说明。 |

`preserve/cli-schema3-20260919` 和近期配置、模型、网络、连续性、观测等分支已是当前祖先。旧配置 stash `92dda8d3` 的统一配置工作已由 `80fe3931` 及后续修复接续，未重新应用旧草稿。

## 本轮验证

- self-reset、CONT store、workflow retention、材料合同：41 项通过，199 个断言。
- tool contract、工具声明/身份、分层诊断、incident CLI（含真实打包 Node CLI）：131 项通过，672 个断言。
- AH-157 配对拒绝反例、唯一 recipe、Host health source adapter：22 项通过，125 个断言；这是本轮离线回归，不替代原分支的原生资格窗口。
- `bun run check:docs`：16 项通过，1679 个断言。
- 根目录和 Web 类型检查、正式构建、`git diff --check` 通过。AH-157 合入后再次执行根目录类型检查及构建。

首次 incident CLI 测试在内置构建阶段触及 15 秒测试超时；单独构建成功后，保持原超时和断言复跑通过。本轮未扩大测试超时或绕过制品构建。

## 保留的历史与完成边界

`main` 的旧 export 删除草稿、`feat/box-runtime` 的早期单包 H1/M1 草稿及原 stash 原样保留。它们不是本轮 v2 的未提交改动；导入会恢复已被多包实现和当前合同替代的布局。没有删除这些工作树、草稿或历史保护引用，也不宣称整个仓库的所有工作树都为空。

本记录只证明本地源码回流。没有推送远端、发布制品、安装候选、启动真实 Provider 或切换现役 Host/modeld。AH-133 原生安全点及 AH-157/AH-118 原生消息资格仍由原票承接，不因合并或离线测试自动 Done。相关边界分别见 [C1 报告](2026-09-22-self-reset-queue.md)和 [核心配对报告](2026-09-23-native-core-pair-requalification.md)。
