# Preload 当前源码绑定验证（2026-09-23）

本报告记录 AH-160 的固定实现窗口，不签整个 release-offline、当前原生部署或后续来源资格。基线为 `9580ddc549559270db5083a773d528476866300b`。

## 复现与修复

原 E09 在当前源码打包后仍要求历史 preload 金值，实测5项通过、1项失败。历史 `8d5c2f5a…` 与重建 `9c06ec5b…` 属于不同输入；没有修改金值求绿。

`preload-artifact.mjs` 复用现有 buildProvenance，并与原 packer 共用唯一编译配方。先以只读描述符捕获候选，再从源码、锁、实际编译器和 SDK 身份独立重建到内存；前后检查候选与输入稳定，比较完整产物字节。它不执行、不覆写候选，也不依据候选自报身份生成预期。打包仍由原 packer 负责，写入自有临时文件后替换产物；没有新增运行版本库。

E09 和 context artifact lane 已一并迁移，静态金值文件退出。实际 lane 在任何候选加载探针前检查绑定，全部测试后再次核对。这个重建检查不是独立编译器、代码安全审计、签名认证或采用许可；运行行为仍由现有测试负责。

## 成功证据

| 入口 | 实际结果与边界 |
| --- | --- |
| `preload-artifact.test.ts` 与原 E09 | 20项通过，14项边界/反例加6项原行为；错误源码/锁/协议/编译器/SDK身份、篡改、截断、空文件、路径异常及读中替换均拒绝；候选保持原字节 |
| `verify-context-continuity.mjs --lane artifact-e2e` | 原 packed35项、observation11项通过，无跳过；E09通过，E07仍是已声明的部分资格，E10/E11及native/live未被代签 |
| `verify-host-health.mjs native-runtime` | 90项、14文件通过，无失败/跳过；原生声明与自有存储，外部Provider/账号能力仍隔离 |
| 类型与协议 | 根/Web类型、现行协议及Rust39项通过 |
| 取消传播/执行寿命独立检查 | 10项通过；不代表其在前一大组超时的触发条件已查明 |

源码/测试/锁1282文件摘要为 `150702cd955abfebebb7d48e49a8080265b1fca67d10206549bae91e0caf7df3`，对应原生与context窗口前后一致。build sourceDigest 为 `c64b50917e53b9c208155521e7aa957aa4d2d357d5828bad7ea869e9c56b6973`，preload 为 `47b1cdb83a07c8fa58ecca1954c12ca2f4a68cc64f2d90eaee1ce1b27e612c12`（638443字节）。使用Bun1.3.14、esbuild0.28.2，原生Node显式指定。包装内外及重复窗口计数不相加。

反例测试首轮编译服务跨测试寿命失效；明确结算setup及每项自有编译服务后通过，没有提高测试预算。定向通过不覆盖所有负载或竞争情况。

## 未通过与未执行的检查

release-offline首先出现取消传播用例15秒超时，随后原180秒总截止终止测试进程，没有完整通过数量。恢复连接后新窗口的core又达到原270秒测试总截止；Rust、类型与协议通过，但不能把未完成的Bun大组写成全绿。两个入口失败均保留原日志，未反复重试或放大预算求绿。

AH-161负责上述完整回归与生命周期调查，阻AH-117和Q最终资格；它不是新的Host来源失配。尝试修改运行器分组的工具调用被平台拒绝，未执行。随后打包、文档和公开性收尾检查的工具调用亦被拒绝，未取得其结果。本报告不宣称这些检查通过，也不把局部验证替代最终签收。

本批没有现役Host/modeld切换、profile发布、真实模型费用或用户Bot/App操作。当前提交、是否合流及剩余责任由Linear AH-160的实际回执记录；历史报告保留其原始输入与结论。
